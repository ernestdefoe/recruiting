import app from 'flarum/admin/app';

// Settings are registered declaratively via the Admin extender exported from
// js/admin.js → src/extend.js.  Nothing extra needed here.
app.initializers.add('ernestdefoe-recruiting', () => {});
